# Gnark ZKP for ZKeeper

```
make install
make run
```

Mysteriously, the proof doesn't verify :-'(

Still, I have some files `verifier.sol` and `verifier.t.sol` in `dark/` that actually pass. I don't know why I am not able to reproduce the result...